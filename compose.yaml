services:

  ganxterm:
    build:
      context: .
      dockerfile: Containerfile
    restart: unless-stopped
    ports:
      - "8765:8765"
    env_file:
      - .env
    environment:
      - GANXTERM_DATA_DIR=/data
    volumes:
      - ganxterm_data:/data

volumes:
  ganxterm_data:
